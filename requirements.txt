# --- Web framework ---
fastapi>=0.115.0
uvicorn>=0.34.0
starlette>=0.46.0
python-multipart>=0.0.20
jinja2>=3.1.0
werkzeug>=3.1.0
pydantic>=2.10.0
python-dotenv>=1.0.0
requests>=2.32.0
httpx>=0.28.0

# --- LangChain / LangGraph (0.3.x ecosystem) ---
langchain>=0.3.28,<1.0
langchain-core>=0.3.75,<1.0
langchain-community>=0.3.28,<1.0
langchain-qdrant>=0.2.0,<1.0
langgraph>=0.3.9,<1.0
langgraph-checkpoint>=2.0.19
langchain-text-splitters>=0.3.6

# --- Google Gemini ---
# Note: google-ai-generativelanguage version is intentionally NOT pinned here;
# langchain-google-genai pulls its own compatible range (older 2.x pulls genai
# <0.7.0, newer versions allow >=0.7.0), avoiding resolver conflicts.
langchain-google-genai>=2.0.0,<3.0.0

# --- Groq (primary LLM provider) ---
langchain-groq>=0.3.0
# Note: langchain-openai intentionally NOT included: it requires langchain-core>=1.x,
# conflicting with the langchain 0.3.x ecosystem pinned in this project. OpenRouter
# fallback stays off here; Groq is the primary provider.

# --- Vector store (local Qdrant + hybrid search) ---
qdrant-client>=1.13.5
fastembed>=0.6.0

# --- Document parsing & OCR (fast, lightweight, pure ONNX, no torch) ---
rapidocr>=3.0.0
pypdfium2>=4.30.0

# --- Optional cross-encoder reranking (requires torch; install only if desired) ---
# sentence-transformers>=3.0.0

# --- Supabase (optional - auth + database) ---
supabase>=2.0.0
psycopg2-binary>=2.9.0

# --- Speech (ElevenLabs) ---
elevenlabs>=1.54.0
pydub>=0.25.1

# --- Utilities ---
numpy>=1.26.0
pillow>=11.0.0
# --- PDF support (pure-python text pull + MuPDF rasterizer for scanned PDFs; no torch) ---
pypdf>=4.0.0
pymupdf>=1.24.0